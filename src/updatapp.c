#ifdef HAVE_CONFIG_H
# include "config.h"
#endif

#define USE(x) ((void)(x))
#include "updatapp.h"

int
updatapp(const char *input_directory, const char *output_file)
{
    USE(input_directory);
    USE(output_file);
    return -1;
}
