#!/bin/bash -xe

rm -Rf output
rm -Rf output_mine

${srcdir}/tests/split_updata.pl UPDATA.APP
${srcdir}/src/unupdatapp UPDATA.APP output_mine

(cd ${srcdir}/tests/output && md5sum *) | (cd ${srcdir}/tests/output_mine && md5sum -c)
exit $?
